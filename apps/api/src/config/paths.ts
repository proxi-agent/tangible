/**
 * Re-exported from `@tangible/analytics`, which is where these live now — the
 * Next.js route handlers resolve the same warehouse path and cannot import from
 * this app.
 */
export { findRepoRoot, resolveDataPath } from '@tangible/analytics';
