node_modules/
.pnpm-store/

# build output
dist/
.next/
out/
*.tsbuildinfo

# turbo
.turbo/

# env
.env
.env.*
!.env.example

# data warehouse — county files and the DuckDB database are large, never commit.
# Anchored: an unanchored `data/` also swallowed apps/web/src/app/data, the
# Data sources route, which then 404'd in every deployment.
/data/
*.duckdb
*.duckdb.wal
*.parquet

# misc
.DS_Store
*.log
coverage/
